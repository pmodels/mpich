name: ci

on:  # yamllint disable-line rule:truthy
  push:
    branches:
      - "main"
      - "[0-9]+.[0-9]+.x"
  pull_request:
    branches:
      - "main"  # TODO: review
      - "[0-9]+.[0-9]+.x"

jobs:

  Linux:
    runs-on: ubuntu-latest
    steps:
    - run: echo ${{ runner.os }}

  macOS:
    runs-on: macos-latest
    steps:
    - run: echo ${{ runner.os }}
