ALL: all-redirect
SHELL = @SHELL@
VPATH = @srcdir@
srcdir = @srcdir@
.SUFFIXES: .pdf .dvi .tex

@VPATH@

LATEX      = export TEXINPUTS=".:$(srcdir):" ; latex
BIBTEX     = export BIBINPUTS=".:$(srcdir):" ; bibtex
DVIPDFM    = export TEXINPUTS=".:$(srcdir):" ; dvipdfm
LATEX2HTML = latex2html

.tex.dvi:
	-$(LATEX) $< </dev/null >/dev/null 2>&1
	-$(LATEX) $< </dev/null >/dev/null 2>&1
	-$(BIBTEX) $* </dev/null
	$(LATEX) $<

.dvi.pdf:
	$(DVIPDFM) $*

all-redirect: logging.pdf

clean:
	-rm -f *.toc *.out *.dvi user.ps user.pdf *.aux *.blg *.bbl *.log

mandoc:

htmldoc:

latexdoc: ALL
